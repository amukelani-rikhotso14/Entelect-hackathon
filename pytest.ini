[pytest]
# OneDrive-backed folders can cause permission issues for pytest's cache/temp dirs.
# Disabling the cache provider and ignoring its fallback dirs avoids collection errors.
addopts = -p no:cacheprovider
norecursedirs =
    .pytest_cache
    pytest-cache-files-*
